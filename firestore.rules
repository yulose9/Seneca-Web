rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // STRICT SECURITY: Only allow YOUR specific email to read/write
      allow read, write: if request.auth != null && request.auth.token.email == 'jannazarene09@gmail.com';
    }
  }
}
